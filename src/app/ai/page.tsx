import { Sparkles } from "lucide-react";
import { ComingSoon } from "@/components/shared/coming-soon";

export default function AiCoachPage() {
  return (
    <ComingSoon
      icon={Sparkles}
      title="AI Coach"
      description="Full conversations with your AI coach are coming in a future phase — try the quick actions from the Ask Ascend button for a preview."
    />
  );
}
